appId: dev.reasonix.desktop
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
productName: Reasonix Desktop
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  # Bundled Node.js for spawning the reasonix CLI
  - from: resources/node
    to: node
    filter:
      - "**/*"
asarUnpack:
  # Unpack the bundled Node binary (can't run from inside asar)
  - resources/node/**
  # Unpack reasonix tokenizer data (loaded by path at runtime)
  - "**/node_modules/reasonix/data/**"
  - "**/node_modules/reasonix/dist/**"
nsis:
  artifactName: ReasonixDesktop-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  oneClick: false # 是否一键安装，开启辅助安装才能选择安装目录
  allowToChangeInstallationDirectory: true # 是否允许用户选择安装目录
  perMachine: true # 是否安装在所有用户的安装目录下
  warningsAsErrors: false
  allowElevation: true
  unicode: true # 确保安装脚本支持中文路径
  include: build/installer.nsh # 👈 引用的自定义 NSIS 脚本 实现安装时选择内部软件安装目录
win:
  executableName: ReasonixDesktop
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: wangzi849727705
  repo: reasonix-desktop
  releaseType: release
